/**
 * Come from internal server
 * That is not for user issues
 * @type {string}
 */
export const INTERNAL_SERVER_ERROR = "Somthing went wrong! Try Again";

/**
 * Come for user wrong credentials input
 * when tring to change account
 * @type {string}
 */
export const CREDENTICALS_INCORRECT = "Invalid Credentials";

/**
 * Agent is already create with same email
 * when tring to create a new agent
 * @type {string}
 */
export const AGENT_EXIST = "This Email is already used";

/**
 * A used promo request
 * when an agent trying to set or update promo
 * @type {string}
 */
export const PROMO_USED = "Try with another Promo";

export const INVALID_VERIFICATION_CODE = "Invalid or expired verification code";
